{{ define "main" }}
  <h1 class="h2 mb-4">{{ .Title }}</h1>

  <ul class="list-group">
    {{ range .Pages }}
      <li class="list-group-item">
        <a href="{{ .RelPermalink }}">{{ .Title }}</a>
      </li>
    {{ end }}
  </ul>
{{ end }}
